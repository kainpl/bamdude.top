---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Nav from '../../components/Nav.astro';
import ScrollProgress from '../../components/ScrollProgress.astro';
import PageHeader from '../../components/PageHeader.astro';
import CompareCards from '../../components/CompareCards.astro';
import CompareTable from '../../components/CompareTable.astro';
import FinalCTA from '../../components/FinalCTA.astro';
import Footer from '../../components/Footer.astro';
import { t } from '../../i18n';
const locale = 'uk' as const;
const i = t(locale);
const p = i.pages.compare;
---
<BaseLayout title={p.metaTitle} description={p.metaDescription} locale={locale}>
  <ScrollProgress />
  <Nav locale={locale} active="compare" />
  <PageHeader eyebrow={p.eyebrow} title={p.title} subtitle={p.subtitle} />
  <CompareCards locale={locale} />
  <CompareTable locale={locale} />
  <FinalCTA locale={locale} />
  <Footer locale={locale} />
</BaseLayout>
